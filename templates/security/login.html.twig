<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Connexion</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-image: url('{{ asset('img/blog3.jpg') }}'); /* Image de fond */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        .form-container {
            background: rgba(255, 255, 255, 0.8); /* Fond semi-transparent */
            padding: 5px 20px; /* Réduction maximale du padding pour diminuer la hauteur */
            border-radius: 10px;
            box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 600px; /* Largeur inchangée */
            min-height: 250px; /* Réduction maximale de la hauteur minimale */
        }

        .form-container .form-control {
            border-radius: 8px;
            border: 1px solid #ced4da;
            padding: 6px; /* Réduire le padding pour diminuer la hauteur des champs */
            transition: 0.3s;
        }

        .form-container .form-control:focus {
            border-color: #007bff;
            box-shadow: 0px 0px 5px rgba(0, 123, 255, 0.5);
        }

        .form-container .btn {
            padding: 8px;
            font-size: 16px;
            font-weight: bold;
            transition: 0.3s;
        }

        .form-container .btn:hover {
            background-color: #0056b3;
        }

        .form-check-label {
            font-size: 14px;
        }

        .alert {
            font-size: 14px;
            border-radius: 8px;
        }

        /* Logo */
        .logo-container {
            text-align: center;
            margin-bottom: 5px; /* Réduction de l'espace autour du logo */
        }

        .logo-container img {
            max-width: 180px; /* Réduire la taille du logo */
        }

        /* Phrase d'accueil */
        .welcome-text {
            text-align: center;
            font-size: 16px; /* Réduire encore la taille du texte */
            font-weight: bold;
            color: #333;
            margin-bottom: 5px; /* Réduire l'espace entre le texte et le formulaire */
        }

        /* Espacement du bouton */
        .form-container .d-grid {
            margin-bottom: 20px; /* Ajouter de l'espace sous le bouton */
        }
    </style>
</head>
<body>

    <div class="form-container">
        <!-- Logo -->
        <div class="logo-container">
            <img src="{{ asset('img/logo.png') }}" alt="Logo de l'application"> <!-- Logo de l'application -->
        </div>

        <!-- Phrase d'accueil -->
        <div class="welcome-text">
            <p>Bienvenue dans notre service, votre bien-être est notre priorité !</p>
        </div>

        <form method="post">
            {% if error %}
                <div class="alert alert-danger">
                    {{ error.messageKey|trans(error.messageData, 'security') }}
                </div>
            {% endif %}

            <div class="mb-3">
                <label for="inputEmail" class="form-label fw-semibold">Email</label>
                <input type="email" name="email" id="inputEmail" class="form-control" required autofocus>
            </div>

            <div class="mb-3">
                <label for="inputPassword" class="form-label fw-semibold">Mot de passe</label>
                <input type="password" name="password" id="inputPassword" class="form-control" required>
            </div>

            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

            <div class="form-check mb-3">
                <input class="form-check-input" type="checkbox" id="remember_me" name="_remember_me">
                <label class="form-check-label" for="remember_me">Se souvenir de moi</label>
            </div>

            <div class="d-grid">
                <button type="submit" class="btn btn-primary">Se connecter</button>
            </div>
        </form>
    </div>

</body>
</html>
