 {% extends '/dashboard/base.html.twig' %}

{% block body %}
    <h1>Créer un patient</h1>

    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}
        {{ form_widget(form) }}
    {{form_errors(form)}}
    {{ form_end(form) }}
{% endblock %}