{% extends 'base.html.twig' %}

{% block title %}Détails du Pharmacien{% endblock %}

{% block body %}
<div class="container mt-5">
    <h1 class="text-center mb-4">💊 Détails du Pharmacien</h1>

    <div class="row">
        <div class="col-md-6 offset-md-3">
            <table class="table table-bordered">
                <tbody>
                    <tr>
                        <th>Nom</th>
                        <td>{{ pharmacien.nom }}</td>
                    </tr>
                    <tr>
                        <th>Prénom</th>
                        <td>{{ pharmacien.prenom }}</td>
                    </tr>
                    <tr>
                        <th>Email</th>
                        <td>{{ pharmacien.email }}</td>
                    </tr>
                    <tr>
                        <th>Numéro de licence</th>
                        <td>{{ pharmacien.numeroLicence }}</td>
                    </tr>
                    <tr>
                        <th>Téléphone</th>
                        <td>{{ pharmacien.telephone }}</td>
                    </tr>
                    <!-- Ajoutez d'autres informations spécifiques au pharmacien ici -->
                </tbody>
            </table>

            <a href="{{ path('admin_users') }}" class="btn btn-secondary btn-lg">Retour</a>
        </div>
    </div>
</div>
{% endblock %}
