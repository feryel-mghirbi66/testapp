{% extends 'base.html.twig' %}

{% block body %}
    <h1>Créer un membre Staff</h1>

    {{ form_start(form,{'attr': {'novalidate': 'novalidate', 'class': 'needs-validation'}}) }}
        {{ form_widget(form) }}
    {{ form_end(form) }}
{% endblock %}
