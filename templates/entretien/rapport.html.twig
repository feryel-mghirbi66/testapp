<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rapport d'Entretien</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            padding: 0;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        .header h1 {
            color: #333;
        }
        .content {
            border: 1px solid #ccc;
            padding: 15px;
            border-radius: 5px;
        }
        .content h2 {
            color: #555;
        }
        .footer {
            text-align: center;
            margin-top: 30px;
            font-size: 12px;
            color: #777;
        }
    </style>
</head>
<body>

    <div class="header">
        <h1>Rapport d'Entretien pour l'équipement : {{ entretien.nomEquipement }}</h1>
        <p>Date de génération : {{ "now"|date("d/m/Y H:i") }}</p>
    </div>

    <div class="content">
        <h2>Détails de l'Entretien</h2>
        <p><strong>ID :</strong> {{ entretien.id }}</p>
        <p><strong>Équipement :</strong> {{ entretien.nomEquipement }}</p>
        <p><strong>Date :</strong> {{ entretien.date|date("d/m/Y") }}</p>
        <p><strong>Description :</strong> {{ entretien.descreption }}</p>
    </div>

    <div class="footer">
        <p>Clinique - Système de gestion des équipements</p>
    </div>

</body>
</html>
