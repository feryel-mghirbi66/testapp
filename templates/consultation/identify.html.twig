 {% extends 'base.html.twig' %}

{% block body %}
    <style>
        /* General styling */
        body {
            font-family: Arial, sans-serif;
            background: linear-gradient(to right, #4CAF50, #2196F3); /* Green to Blue gradient */
            color: #333;
            margin: 0;
            padding: 0;
            background-size: cover;
        }

        h1 {
            color: #2d4b61; /* Dark blue */
            text-align: center;
            margin-top: 50px;
        }

        /* Form styling */
        form {
            max-width: 600px;
            margin: 30px auto;
            background-color: rgba(255, 255, 255, 0.9); /* Semi-transparent white */
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
        }

        button {
            background-color: #0099cc; /* Cool blue */
            color: white;
            border: none;
            padding: 12px 24px;
            font-size: 1em;
            cursor: pointer;
            border-radius: 5px;
            transition: background-color 0.3s ease;
            width: 100%;
            margin-top: 20px;
        }

        button:hover {
            background-color: #0077b3; /* Darker blue on hover */
        }

        /* Form row styling */
        .form-group {
            margin-bottom: 15px;
        }

        .form-group label {
            font-weight: bold;
        }

        /* Responsiveness */
        @media (max-width: 768px) {
            form {
                width: 90%;
                padding: 15px;
            }
        }
    </style>

    <h1>Creer Consultation</h1>

     {{ form_start(form, {'attr': { 'novalidate': 'novalidate'}}) }}
        {{ form_row(form.service) }}
        {{ form_row(form.date) }}
        {{ form_row(form.patientIdentifier) }}  {# If you have patientIdentifier field #}
        <button type="submit">Submit</button>
    {{ form_end(form) }}
{% endblock %}

