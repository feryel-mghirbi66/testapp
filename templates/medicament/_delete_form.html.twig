<form method="post" action="{{ path('app_medicament_delete', {'id': medicament.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer ce médicament ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medicament.id) }}">
    <button class="btn btn-danger btn-sm">Supprimer</button>
</form>
